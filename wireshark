
Common use of wireshark

##################################
We can decrypt a TLS ; SSL packet with wireshark if we got the key:
Edit > Preferences > Protocols > TLS >  [+]
The SSLkeylogfile can be find in the variables environnment :
On Windows enter "env" and then "Environment Variables..."



Find the http packets that upload files
http.request.method == "POST"  && http.content_type contains "multipart/form-data"
